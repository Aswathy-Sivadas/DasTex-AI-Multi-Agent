services:
  redis:
    image: redis
    ports:
      - 6379:6379
  mongodb:
    image: mongo:7
    ports:
      - 27017:27017
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=dastex

volumes:
  mongodb_data: